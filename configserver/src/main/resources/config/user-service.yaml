spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fitness-micro-user?createTableIfNotExists=true
    username: root
    password: root@123
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: false

server:
  port: 8081

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/